# Ternary Operator are also known as Conditional Expressions

# Mechanism:

# Condition_if_true if Condition else Condition_if_false

is_friend = True
can_message = "message allowed" if is_friend else "not allowed to message"

print(can_message)
